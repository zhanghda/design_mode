装饰者模式

装饰者模式是为已有功能动态添加一些功能提供的一种方式
应用场景: 当需要向老系统添加新功能的时候，向旧的代码中添加新的代码，这些新的代码装饰了原有类的主要职责和核心行为。

这样就会在主类上面新增很多新的属性和方法，从而提高了程序的复杂度，装饰着模式提供了一个很好的解决方案，只要把每个功能都放到单独的类中，
并让这个类包装索要装饰的对象，因此当要执行特殊行为时，客户端代码就可以有选择有顺序的装饰对象了。

核心思想：主要是把包装类从要包装的对象类中移除，可以简化原有的类了，从而去除相关类中重复的装饰逻辑
